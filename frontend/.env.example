# Supabase Edge Function endpoints. When both are set, /api/compare-prices and
# /api/destinations call the Edge Functions first and fall back to Flask on
# error. Leave empty to keep using Flask exclusively.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
